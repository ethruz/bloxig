<%- include('../layout/header') %>
<section style="min-height: calc(100vh - 56px); display: flex; align-items: center; justify-content: center; padding: 48px 20px; text-align: center;">
  <div>
    <p style="font-size: var(--text-xs); font-family: var(--font-mono); color: var(--text-tertiary); margin-bottom: 16px; letter-spacing: 0.1em;">404</p>
    <h1 style="font-size: var(--text-2xl); letter-spacing: -0.03em; margin-bottom: 12px;">Page not found</h1>
    <p style="font-size: var(--text-sm); color: var(--text-tertiary); margin-bottom: 32px;">The page you're looking for doesn't exist.</p>
    <a href="/" class="btn btn--ghost">Go home</a>
  </div>
</section>
<%- include('../layout/footer') %>
